#!/bin/sh

set -x
cd .ypkg2/

[ -d ../bin ] && cp -R ../bin .
. .pkginfo

filelist=".pkginfo bin"
filename="$name.$version.$arch.$os.ypkg2.tar.zst"

tar -c --zstd -f "$filename" $filelist
mv "$filename" ../pkg/
